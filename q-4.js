//? Write a function that generates a random number between 1 and 10, and returns a promise that resolves with the random number if a user submitted number is divisible by the generated random number, and rejects otherwise.

const formEle = document.querySelector("form");

function randomNumber() {
  // Your code here
}

randomNumber()
  .then((number) => {})
  .catch((error) => {});
